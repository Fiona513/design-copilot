# V2.1 测试结果

执行日期：2026-09-02

执行命令：`node --test tests/*.test.mjs`

结果：9 tests / 2 suites，全部通过。

## 双项目差异验证

### A. ADHD 儿童阅读产品

- Brief：ADHD 儿童低认知负荷阅读产品；问题聚焦开始阅读与分心恢复。
- Insight：单一主行动、短句、稳定层级和低刺激反馈。
- Principles：One decision at a time / Return without penalty。
- Flow：Home → Choose Book → Reading → Pause → Resume Exact Page → Complete Chapter。
- Screen Structure：Home / Reading / Completion。
- Prototype：FOCUS READER、章节页码、恢复阅读和章节完成反馈。
- Review：检查“恢复阅读”是否与次级控制同级。

### B. 跨境电商运营后台

- Brief：跨境电商运营后台；问题聚焦异常订单、库存冲突与批量履约。
- Insight：风险优先、批量影响确认、并发状态和审计追踪。
- Principles：Risk before volume / Bulk with confidence。
- Flow：Operations Overview → Review Alert → Open Orders → Select Orders → Confirm Live Status → Apply Bulk Action。
- Screen Structure：Operations Overview / Order Workbench / Operation Result。
- Prototype：SELLER COMMAND CENTER、异常订单、批量处理和冲突结果。
- Review：检查“批量订单操作”是否与次级操作同级。

上述六类设计产物和 Review 均断言为明显不同；Prototype V2 均基于具体 Issue 修改并通过第二轮 Review。

## 可靠性验证

- 每个 Provider 请求都携带 strict JSON Schema，`store: false`，不请求 Chain of Thought。
- 模型返回非法 JSON 时重试一次，第二次合法结果通过 Schema 校验。
- 连续上游 500 返回结构化 `AI_API_ERROR`；不写入 Task State。
- 无 Key 返回 `AI_NOT_CONFIGURED`，浏览器自动使用 LocalDemoProvider 并显示 fallback。
- Retry AI 失败后保留当前状态；Use Local Demo 可继续相同操作。
- Ask Agent 修改完整 Artifact；Regenerate 产生新的 generation 并再次调用 Provider。
- Apply Fix 保留 Prototype V1，新增 Prototype V2 与 iteration，再运行 Round 2 Review。
- Stop 会 abort 正在进行的模型请求，Brief 和后续产物均不会被迟到响应写入。
- 完成项目后写入 History；重新加载后 V2、Reviews 与 runtime 状态正常恢复。

## Render Web Service 验证

- `pnpm start` 成功启动 `node server.mjs`，日志显示 `0.0.0.0:<PORT>`。
- `GET /`、`/v2.css` 与 `assets/branding/agent-logo.png` 返回 200。
- `GET /health` 与 `GET /api/health` 返回 200。
- 未配置 Key 时 `POST /api/agent` 返回结构化 `AI_NOT_CONFIGURED`（503）。
- `node --check app.js`、`server.mjs`、`provider-schemas.mjs` 全部通过。

## Live API

本机测试环境没有 `OPENAI_API_KEY`，因此没有产生真实 OpenAI 计费调用。Proxy 请求格式、Responses API 响应解析、严格 JSON 校验和前端端到端接线使用本地协议级 mock 完整执行。配置 `.env` 后即可运行同一套真实模型路径。
