# V2.1 修改说明

## Implemented

- 新增 `LLMProvider`，统一实现 Project Understanding、Insights、Principles、Flow、Screen Structure、Prototype、Review 与 Artifact Revision。
- 新增 `ProviderGateway`：默认调用真实模型，保留 `LocalDemoProvider` 作为显式 fallback / demo mode。
- 新增最小 Node API proxy `server.mjs`，仅负责接收请求、调用 OpenAI Responses API、校验严格 JSON Schema、返回安全结果与静态文件。
- 新增 `provider-schemas.mjs`，为所有模型节点及 Artifact Revision 定义独立 Schema；Review 额外校验六个类别恰好各出现一次。
- API Key 只从服务端 `.env` 读取；新增 `.env.example` 和 `.gitignore`。
- Runtime 改为真实异步请求，保留原 Task State 和 Golden Path；Stop 会中止 fetch 并使迟到响应失效。
- Missing Context 改由 Provider 根据完整 Project Context 判断。
- Ask Agent 返回完整替换 Artifact；Regenerate 重新请求 Provider；Apply Fix 根据具体 Review Issue 生成 Prototype V2 并可 Re-review。
- 加入 loading、结构化 API error、Retry AI、Use Local Demo、fallback 提示与 provider/model 元数据。
- Prototype 外壳不变，但三屏内容由结构化 `ui` 数据驱动，电商、效率、阅读和未知产品不再共用 ADHD 文案。
- 保持 Approval、Replan、Review、History、localStorage、Export 与 Stop/Resume 行为。
- Render 适配：监听 `0.0.0.0`，端口优先读取 `PORT`、本地默认 3000；新增 `/health`；加入单 IP 限流、1 MB 请求体限制、最大输出 tokens、超时和结构化错误处理。

## Files

- `app.js` — Providers、runtime、真实异步节点与现有状态机接线
- `server.mjs` — 最小安全 API proxy
- `provider-schemas.mjs` — JSON Schemas 与本地校验
- `.env.example` — Key、模型和超时配置模板
- `tests/server-integration.test.mjs` — Proxy、结构化输出和双项目测试
- `tests/frontend-state.test.mjs` — Golden Path、Revision、Fallback、Stop 与持久化测试
- `README.md` — 运行、模型配置和 Provider 说明
- `.env.example` — Render/本地端口、Host、限流和模型配置模板

## Intentionally unchanged

- 页面数量、三栏布局和视觉语言
- Golden Path 与 Plan/Approval/Replan/Review/History 状态逻辑
- localStorage 数据持久化与 JSON Export
- V1/V2 原型切换和人工 Apply Fix / Ignore 决策
